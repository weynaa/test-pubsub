syntax = "proto3";

import "google/protobuf/empty.proto";

message SubMessage{
	int32 value = 1;
}

service PubSub {
  // Sends a greeting
  rpc subscribe(google.protobuf.Empty) returns (stream SubMessage) {}
}